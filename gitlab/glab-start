#!/bin/sh
if [ -z "$1" ]
then
  echo "No issue number supplied"
  exit 1
fi

glab mr create -a eiand --related-issue $1 --fill --yes
glab issue update $1 -l "status::in progress" -a eiand

mr_id=$(glab mr view | awk '/number:(.*)/' | awk -F':' '{print $2}' | xargs)
glab mr checkout $mr_id